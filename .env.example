# KicksDB (api.kicks.dev) - sneaker marketplace data API
KICKSDB_API_KEY=changeme
KICKSDB_BASE_URL=https://api.kicks.dev

# Postgres
POSTGRES_DB=sneaker_wishlist
POSTGRES_USER=sneaker_wishlist
POSTGRES_PASSWORD=changeme

# Optional - only needed if you change the default ports in docker-compose.yml
# CORS_ALLOWED_ORIGINS=http://localhost:5173
# VITE_API_BASE_URL=http://localhost:8080
