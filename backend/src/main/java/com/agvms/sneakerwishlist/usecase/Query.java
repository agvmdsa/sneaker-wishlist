package com.agvms.sneakerwishlist.usecase;

/**
 * Marker interface for a read request that returns {@code R}. Implementations
 * are simple records wrapping the input a {@link QueryHandler} needs to execute.
 */
public interface Query<R> {
}
