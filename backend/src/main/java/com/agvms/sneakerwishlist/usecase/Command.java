package com.agvms.sneakerwishlist.usecase;

/**
 * Marker interface for an intent to change state. Implementations are simple
 * records wrapping the input a {@link CommandHandler} needs to execute.
 */
public interface Command {
}
